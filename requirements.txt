# Core (all HPO-B experiments)
numpy>=1.21
scipy>=1.7
scikit-learn>=1.0

# Live-training validation only (real_training_runner.py, multi_learner_runner.py)
xgboost>=1.5
openml>=0.12
pandas>=1.3

# HPO-B itself is not on PyPI: clone https://github.com/machinelearningnanoscale/HPO-B
# and point HPOB_REPO / HPOB_DATA_ROOT at it (see README).
